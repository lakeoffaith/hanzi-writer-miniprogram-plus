<view 
  class="custom-button {{type}} {{size}} {{disabled ? 'disabled' : ''}} {{pressed ? 'pressed' : ''}}"
  bindtap="handleTap"
  bindtouchstart="handleTouchStart"
  bindtouchend="handleTouchEnd"
>
  <text class="button-text">{{text}}</text>
</view>
